# plasma: 粉尘等离子体方案 no-go 筛查模型

本目录用于论文前期粉尘等离子体裂变碎片推进方案的一阶可行性筛查。它建立了一个低密度 DustCloud、轴向磁场约束、裂变碎片代理源项和热归一化评估链路，用于判断该方案在当前参数口径下是否能同时满足裂变碎片逃逸、临界质量代理、热设计质量和辐射冷却闭合要求。

本目录的定位是“前期概念方案设计与参数一致性 no-go 论证”，不是最终可行推进器方案。

## 研究目的

该模型用于回答以下问题：

- 低密度粉尘云是否有利于裂变碎片输运和逃逸；
- 该低密度活动云的质量是否接近临界质量代理或热设计燃料质量；
- 在给定热功率和归一化时间窗下，DustCloud 内的能量沉积能否达到目标温升；
- 在目标温度和高温稳定性检查温度下，粉尘颗粒辐射冷却是否闭合；
- 如果上述约束无法同时满足，是否应将该路线作为后续主方案排除。

## 当前代码实现

主要实现位于 `src/` 和 `include/`：

- `DetectorConstruction.cc`
  - 建立 World、Envelope、BeO 外壳、放电腔、膨胀段、漂移段、线圈和电极等概念几何；
  - 将 `DustCloud` 建模为半径 20 cm、总厚度 40 cm 的低密度粉尘云；
  - `DustCloud` 使用 `DustyPlasma` 材料，平均密度为 `1.0e-4 g/cm3`；
  - 将 DustCloud 设为能量沉积评分体；
  - 区分粉尘云平均密度和固体燃料颗粒密度。

- `DetectorConstruction.hh`
  - 保存热筛查参数：
    - 参考初始粉尘温度 2800 K；
    - 目标温度 3080 K；
    - 高温稳定性检查温度在 `RunAction` 中取 3800 K；
    - 有效比热容 300 J/(kg*K)；
    - 单次裂变能量释放 207 MeV；
    - 裂变碎片能量份额 0.81；
    - 参考热功率 350 MWth；
    - 归一化时间窗 1 us；
    - 临界质量代理 11 kg；
    - 热设计燃料质量参考值 15 kg；
    - 固体燃料颗粒密度 14.3 g/cm3。

- `PrimaryGeneratorAction.cc`
  - 使用 Ba-140/Kr-95 裂变碎片对作为代理源项；
  - 重碎片平均能量 70 MeV，轻碎片平均能量 95 MeV；
  - 片段电荷态取 +22e；
  - 两个碎片沿近似反向方向发射，并加入位置、角度和能量展宽；
  - 该源项不是完整裂变产物谱。

- `ThrusterMagneticField.cc`
  - 实现轴向引导场和多个高斯型磁场峰；
  - 磁场用于裂变碎片输运约束/导向，不代表完整工程磁体设计。

- `SteppingAction.cc` 和 `EventAction.cc`
  - 统计 DustCloud 内的能量沉积；
  - 按事件汇总后交给运行级热归一化。

- `RunAction.cc`
  - 将 Monte Carlo 能量沉积按 350 MWth 和 1 us 时间窗归一化；
  - 计算归一化评分功率、温升、目标温度达成率；
  - 计算活动粉尘云质量与 11 kg 临界质量代理、15 kg 热设计燃料质量的比例；
  - 估算目标温度和高温稳定性温度下的颗粒辐射冷却余量；
  - 输出 no-go evidence dashboard 的 PASS/FAIL 判据。

- `tools/no_go_evidence_report.py`
  - 运行 `exampleB1`，解析全局运行摘要；
  - 可生成结构化证据表、完整运行日志和 SVG dashboard。

## 证据输出

本目录的源码会在运行结束时输出 no-go 筛查摘要。若需要把运行摘要整理为论文证据材料，可使用 `tools/no_go_evidence_report.py` 生成以下类型的文件：

- 结构化 PASS/FAIL 证据表；
- 完整运行日志；
- 可视化 no-go dashboard。

这些输出文件属于运行生成物，不是源码编译所必需的文件。若仓库中未保留历史结果，应以重新运行生成的日志和证据表为准。

已有 no-go 筛查结果的关键数值为：

- 加热充分性失败：目标温度达成率约 4.01206%；
- 临界质量代理失败：活动云质量约为 11 kg 参考质量的 0.0456959%；
- 热设计质量闭合失败：活动云质量约为 15 kg 参考质量的 0.0335103%；
- 3080 K 目标温度冷却闭合失败，过载因子约 1.88885；
- 3800 K 稳定性温度冷却检查通过，过载因子约 0.815204；
- 从 2800 K 升至 3080 K 的估算时间约 24.9249 us；
- no-go 判据为 `CONFIRMED`。

这些数值应能追溯到作者保留的运行日志或重新生成的证据表；若对应结果文件未随仓库保留，应在论文中注明数据来源或重新运行生成。上述结论不应扩展为“完全否定所有粉尘等离子体裂变碎片推进研究”。

## 构建与运行

典型构建流程如下，前提是本机 Geant4、数据集和编译环境已经正确配置：

```bash
cd plasma
cmake -S . -B build
cmake --build build
```

当前源码根目录只保留可视化宏 `vis.mac`。从干净构建开始运行时，需要自行准备批处理宏文件。例如可写入 `build/run_no_go.mac`：

```bash
cat > build/run_no_go.mac <<'EOF'
/run/initialize
/control/verbose 2
/run/verbose 2
/run/printProgress 100
/run/beamOn 1000
EOF
```

批处理运行示例：

```bash
./build/exampleB1 build/run_no_go.mac
```

生成 no-go 证据表和 dashboard 的脚本示例。下面的输出路径是运行时新建路径，可按需要替换：

```bash
python3 tools/no_go_evidence_report.py \
  --build-dir build \
  --macro run_no_go.mac \
  --log-out build/evidence/<run-log>.log \
  --csv-out build/evidence/<evidence-table>.csv \
  --svg-out build/evidence/<dashboard>.svg
```

注意：重新运行时建议显式传入新的 `--build-dir`、`--macro` 和输出路径，避免混淆历史证据和新运行结果。

## 结果如何解释

plasma 的输出不是推进器最终性能，而是 no-go 筛查证据。论文写作时应区分以下层级：

- 代码已实现：几何、DustCloud 材料、Ba-140/Kr-95 代理源项、磁场、能量沉积统计和热归一化；
- 运行输出已得到：运行日志和证据表中的 PASS/FAIL 指标；
- 谨慎推断：在当前参数一致性筛查和一阶热闭合条件下，粉尘等离子体方案难以同时满足裂变碎片逃逸、临界质量和热管理要求，因此不作为后续主要设计路线；
- 后续工作：若继续研究该方向，需要更高保真的临界计算、完整裂变产物谱、等离子体稳定性、颗粒动力学和热工模型。

## 模型边界

当前模型没有显式求解：

- 完整反应堆临界和中子学闭合；
- 完整裂变产物谱和产额分布；
- 粉尘等离子体稳定性、鞘层、电荷交换和集体效应；
- 颗粒蒸发、烧蚀、团聚和多相流动力学；
- 完整磁体工程设计和线圈功耗；
- 完整喷管、电极能量回收和飞行器总体性能。

因此，推荐结论表述为：

在本文采用的参数口径和一阶筛查条件下，该方案难以同时满足裂变碎片逃逸、临界质量和热管理要求，因此本文不将其作为后续主方案；该结论并不等同于完全否定所有粉尘等离子体裂变碎片推进研究。
